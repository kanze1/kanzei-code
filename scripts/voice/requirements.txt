# vLLM-Omni and vLLM must share the same release series. Linux / WSL2 + NVIDIA.
vllm==0.28.0
vllm-omni==0.28.0
# Match the cu130 PyTorch wheel; unconstrained nvcc can pull incompatible 13.4 headers.
cuda-toolkit[nvcc,cccl]==13.0.3.0
nvidia-cuda-nvcc==13.0.88
nvidia-cuda-crt==13.0.88
nvidia-nvvm==13.0.88
nvidia-cuda-cccl==13.0.85
faster-whisper==1.2.1
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx>=0.28,<1
