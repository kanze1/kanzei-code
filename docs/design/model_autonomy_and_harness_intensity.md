# 模型自治与门禁强度

- 身份: live_design
- 状态: 设计基线
- 日期: 2026-08-21
- 最近核验依据: 6505dfb8
- 关联需求: R-322(门禁强度分档与模型停机权)、R-323(工具编排抽象层)
- 关联缺陷: D-661(wave 调度器保守封波)、D-662(托管文档专用工具膨胀)
- 关联决策: 待转 A-*(本文最终方案经用户 2026-08-21 定调,决策条目随实现落地补)

## 背景与问题

2026-08-21 一轮外部架构评估对 harness 提出七点质疑。评估者的核心论点是:
本仓把**大量 intelligence 放在 harness 侧**,这在弱模型时代是优势,在强 Agent 模型
时代未必——`harness 的保守规则可能成为模型能力的上限`。

七点原始质疑与用户逐点定调:

| # | 质疑 | 用户定调 | 落点 |
| --- | --- | --- | --- |
| 1 | Harness Tax:每个行为都穿过大量 infrastructure,小任务显得重 | 按复杂度路由**有必要**,但**不应该机械** | R-322 |
| 2 | 牺牲了模型的聪明:该并行的、该停的、该总结的,harness 替模型决定了 | 提供底层工具 + **一层抽象层**,让模型去编排 | R-323 |
| 3 | Determinism 换掉了最大并行度 | **算缺陷**,不算决策 | D-661 |
| 4 | Event Sourcing 让小改动变大改动 | 系统级 loop 之外**要有简单 loop**;结伴=有人监督→控制可以轻,重控制是为了监督**过夜自主开发的信息熵增**;现在区别不够明显,**决策点要呈现给用户** | R-322 |
| 5 | Managed Documents 造成 Tool Explosion | **算缺陷**,不算决策 | D-662 |
| 6 | Memory Control Plane 复杂度增速可能高于收益 | **暂时无法确定**,挂起观察 | 见 §TODO |
| 7 | Auto Run 的双控制器问题:谁拥有最终控制权 | **决定权交给模型** | R-322 |

## 目标与非目标

### 目标

1. 把「门禁强度」从**隐含在 agent 人格里的副作用**提升为**一等显式维度**,
   与人格解耦、对用户可见、可按模式取不同默认值。
2. 收回引擎对**任务是否完成**的否决权,交还模型;引擎只保留**资源类**兜底。
3. 为模型提供声明式的编排通道(先落并行分组),引擎从「独裁调度」退为「安全校验」。
4. 修掉两条被判定为缺陷的实现问题(D-661/D-662)。

### 非目标

- **不**回退「判定下沉引擎」这一基线。规则仍然写在代码里,不回到可编辑提示词
  (D-120/D-128/D-163 的教训不因本轮调整而失效)。变的是**哪些判定属于引擎**,
  不是**判定写在哪里**。
- **不**削弱结果侧托管围栏、权限硬门禁与事件真源。这三样与「模型自治」正交:
  它们约束的是**副作用边界**,不是**任务判断**。
- **不**引入第二套开关体系。强度是**一个**枚举,不是 N 个布尔值。

## 讨论摘要

评估把两件事混在一起谈,拆开后结论不同:

- **「规则写在哪里」**:引擎侧。这条有证据(D-120/D-128/D-163 三次现场),不动。
- **「哪些事该由引擎判断」**:分情况。可拆成两类——

| 判断类型 | 举例 | 归属 | 理由 |
| --- | --- | --- | --- |
| **任务判断** | 任务完成了吗、下一步做什么、这两个调用能不能并行 | **模型** | 需要语义理解;引擎只有工具名和锁键,信息量本就不足 |
| **资源判断** | 限流了吗、连跑多少轮了、真实状态还在不在变、上下文超线了吗 | **引擎** | 与语义无关,纯机械可判定;且无人监督时没有第二个判断者 |

现行 `auto_run` FSM 的十种停止原因里,只有 `NoAction`/`Nudge` 属于任务判断,
其余(`AllBlocked`/`BacklogEmpty`/`MaxRounds`/`RepeatedFailure`/`FatalError`/
`RateLimited`/`ZeroOutput`/`Paused`/`StopAfterRound`)都是资源判断或用户意图。
所以 #7 的落点很窄:**只需要交出 `Nudge` 这一条**。

`ZeroOutput`(D-583)看起来像任务判断,实则不是:它比对的是 (HEAD、worktree hash、
tracker 文档内容) 的真实签名,不解释语义,只回答「磁盘上还在不在变」。无人监督时
这是唯一的失控兜底,保留。

## 候选方案

### 方案 A:按 agent 人格继续隐式分档(现状)

差异只落在系统提示词与 `auto_allowed` 的二值条件。
- 优点:零改动。
- 缺点:用户看不见自己选了什么;新增机制时只能再长一个布尔开关
  (`phase_pipeline_enabled` 已是第二个);无法表达「结伴但要重门禁」。
- **不采用**(正是 #4 指出的问题)。

### 方案 B:按任务复杂度自动路由

轮首估算复杂度(文件数/预计步数/条目复杂度字段),自动选强度。
- 优点:用户零操作。
- 缺点:**机械**。用户明确否决:「根据复杂度路由是有必要的,但是不应该机械」。
  且复杂度的真实判断者是模型,不是引擎的启发式。
- **不采用**。

### 方案 C:显式强度枚举 + 模式默认值 + 模型可申请 + 用户可见(选定)

- 强度是一等概念 `HarnessIntensity { Paired, Autonomous }`,由**模式**给默认值;
- 模型在轮内可以**声明**(完成、并行分组),引擎按**单调收紧**规则校验;
- 决策点在界面呈现,用户随时可覆盖。

复杂度路由通过「模型声明」间接实现——模型知道这次是改一个 CSS 还是重构一个子系统,
它来说,引擎不猜。这满足了「有必要但不机械」。

## 最终方案

### 一、门禁强度(R-322)

```rust
pub enum HarnessIntensity {
    /// 结伴:用户在场监督。模型自治优先,引擎只保留资源类兜底。
    Paired,
    /// 自主:无人监督。全套门禁——目标是抑制长时间无人值守的信息熵增。
    Autonomous,
}

pub struct IntensityPolicy {
    /// 引擎能否在模型本轮无实质动作时追加推进指令(Nudge)。
    pub engine_nudge: bool,
    /// 冗余机械提醒([冗余提醒] 就地追加)。
    pub redundancy_hints: bool,
    /// 每关闭 N 条插入的只读验收核查轮。
    pub verify_rounds: bool,
}
```

| 机制 | Paired | Autonomous | 归类 |
| --- | --- | --- | --- |
| 模型声明完成 → 停 | **是** | **是** | 任务判断,永远归模型 |
| 引擎 Nudge | 否 | 是 | 任务判断,仅无人监督时借给引擎 |
| 冗余提醒 | 否 | 是 | 用户在场时自己就看得见重复 |
| 验收核查轮 | 否 | 是 | 用户在场时由用户验收 |
| 失败召回注入 | 是 | 是 | 纯增益,不占决策权 |
| backlog 空/全阻塞 → 停 | 否 | 是 | 自主档取活来自队列;结伴档的活来自用户消息 |
| 目标条件挂着时的停止规则 | 模型判定 | 模型判定 | 目标压过两档的默认判据,含 backlog |
| 资源类停止(限流/致命/连数/ZeroOutput) | 是 | 是 | 资源判断,两档都保留 |
| 权限硬门禁 / 托管围栏 / 事件真源 | 是 | 是 | 副作用边界,与自治正交 |

### 二、模型停机权(R-322 / #7)

新增 `AutoStopReason::ModelDeclaredDone`。判定顺序里插在**用户意图与资源兜底之后、
任务判断之前**:

```
backlog → auto_allowed → halted → paused → stop_after_round → max_rounds
  → round_failure → 【model_declared_done】→ no_action/Nudge → zero_output
  → verify_round → Continue
```

模型的声明通道:`work` 工具新增 `handoff` 动作。选 `work` 而不是新工具,是因为
D-662 已经把「工具膨胀」判成缺陷——`work` 本就是取活/工作编排的抽象面,
循环控制归它语义自洽,且不扩大模型的工具选择面。

声明的采集走**事件流**,与 D-654 同一套路(`ToolStart` 登记意图 → `ToolEnd` ok
才计数),不扫消息历史:轮中上下文压缩会结构性删短消息列表,切片会错位。

### 三、编排抽象层(R-323 / #2 / D-661)

分两步,本轮只做第一步:

1. **D-661 先修**:`build_tool_execution_waves_with` 从「顺序扫描遇冲突即封波」
   改为**冲突前驱层级调度**——每个调用的最早可入波 = 所有更早冲突前驱所在波 + 1,
   从该波起找第一个未满的波。顺序遍历 + 最早可用波,确定性保持。

   **不是 first-fit 装箱**(方案评审时否决):朴素 first-fit 会把调用塞进比其冲突
   前驱更早的波,等于把两次冲突操作颠倒过来。`ToolConcurrency::conflicts_with`
   表达「不能同时跑」而它蕴含「顺序有意义」——同树两次 `WorktreeWrite` 是两次写,
   `Shared` 与 `WorktreeWrite` 是读与写,先后颠倒都换结果。因此必须保住不变式:

   > 对任意 `i < j`,若 `calls[i]` 与 `calls[j]` 冲突,则 `wave(i) < wave(j)`。

   评估者原始举例(`A↔B`、`B↔C` 冲突,`A` 与 `C` 不冲突,期望 `[[A,C],[B]]`)
   **不成立**:它要求 `C` 跑在 `B` 之前,而两者冲突。真实见证用例换成
   `[A,B,C,D]` + `A↔B`、`C↔D`:旧实现 `[[A],[B,C],[D]]` 三波,新实现
   `[[A,C],[B,D]]` 两波,且不颠倒任何冲突对。
2. **R-323 B1 先审计工具契约,而不是先造声明通道。**

   勘察发现 30+ 工具里有 13 个根本没实现 `concurrency()`,走的是 `Exclusive` 默认。
   而那个默认的原意写得很清楚:「旧工具**未显式审计**前绝不自动并行」——
   它是**信息缺失**的保守占位,不是安全断言。#2 说的「harness 的保守规则可能成为
   模型能力的上限」,在这里是字面成立的:三条 `webfetch` 天然可并行,却因为没人
   填过这张表而串行。

   让模型去补一张工具作者本就该填的表,是把责任推错了地方。所以先审计:

   | 工具 | 审计结论 | 契约 |
   | --- | --- | --- |
   | `webfetch` | 生产路径只读(写全在 `mod tests` 之后) | `Shared` |
   | `frontend_locate` / `frontend_check` | 只读探查源码 | `Shared` |
   | `todowrite` | 完全无状态,校验入参后直接回 display | `Shared` |
   | `websearch` | **按入参分流**:带 `prior_art_topic` 时会读-改-写轮次预算 | 无 topic `Shared`;有 topic `WorktreeWrite("prior-art:…")` |

   `websearch` 那条是审计的价值所在:一刀切成只读会让两个同 topic 的并发调用
   互相吃掉对方对轮次预算的写入。锁键用 prior-art 专属前缀而不是工作树键——
   预算落在 `.kanzei/research/`,拿工作树键会让它和 `edit`/`bash` 无谓互斥。

   **仍为 `Exclusive` 的**:`question`(用户交互,天然串行,结论正确)、
   `process`(后台注册表变更)、`prior_art`、`research_index/plan/verify/write`、
   tracker 四件套、`work`。这些都真的有共享可变状态(托管文档 ID 引擎、
   state.db、进程注册表),逐个审计的收益与风险都比上面四类高,留后续批次。

3. **声明通道的必要性待重估(不预先建)。** 审计之后,剩下的 `Exclusive` 大多是
   **真有状态**,不是信息缺失——模型对它们的并行安全性并不比引擎知道得多。
   而纯收紧方向(模型声明「B 必须在 A 之后」)在当前契约下几乎无用武之地:
   同一棵树上读与写、写与写本来就已经冲突。所以本条目**不预先造声明通道**,
   等出现真实的「引擎判错了、模型知道对的」现场再做——按本仓惯例,
   没有消费者的能力不算交付。

### 三点五、轻档 loop 的可达性(R-322 B2)

B1 落地后发现强度策略在生产路径上不可达:`auto_allowed` 要求 `agent == "dev"`,
结伴档永远先 `Stop(ProfileMismatch)`。B2 把判据从 **agent 放宽到 profile**——
dev 两档都能续跑,**区别落在强度而不是能不能跑**;research/readonly 仍拒绝。

同时发现 `backlog` 检查排在最前,而它是**自主档**的取活真源:结伴档的活来自
用户上一条消息,拿队列空当停机判据会让轻 loop 一开就死。故 `backlog_stops_loop`
也进策略表,只对自主档为真。

轻档 loop 的完整形状因此是:**有实质动作就续跑,一轮没动作就交还(不 Nudge),
模型声明完成即停**;资源类兜底(限流/致命/连数/ZeroOutput)一条不少。

连带撤回 R-224 的「结伴勾鞭挞自动切 dev-auto」——那条的前提是结伴档不能 loop,
前提已消失,详见 `interaction_modes.md` 的 2026-08-21 修订说明。

### 三点六、目标条件 loop(R-322 B3)——结伴档的停止规则

B2 让结伴档能 loop 之后,它的停止规则仍然是引擎猜出来的:「这一轮有没有实质动作」。
那还是**引擎在判断任务状态**,只是判得比自主档松。用户 2026-08-21 指向
Claude Code 的 `/goal` 作为参照,定调把停止规则本身也交出去。

形状:

| 角色 | 职责 |
| --- | --- |
| **用户** | 写下停止条件(「全部测试通过且已提交」) |
| **模型** | 判断条件达没达成,达成了就 `work handoff` 声明 |
| **引擎** | 在达成之前不让它散场;复述条件,不发明工作;资源兜底 |

挂上目标后停止规则整体换掉:

- `backlog` 空/全阻塞**不再停机**——目标才是工作来源,队列剩几条与它无关;
- 一轮没有实质动作**不再停机**,改为复述目标再推一轮(`AutoRunAction::GoalPending`);
- 只有**模型声明达成**(`Stop(GoalMet)`,目标自动清除)、**用户喊停**、
  或**资源兜底**才停。

与 `Nudge` 的分水岭是**文案来源,不是行为**:`nudge_prompt()` 的内容是引擎发明的
(「从 defects.md 最上面一条开始找活干」),`GoalPending` 复述的是用户原话。
前者是引擎替模型决定该干什么——正是 #7 双控制器问题的来源;后者用户给了目标、
模型自己判断,控制权仍在模型手里。

两道兜底(缺一不可,因为目标 loop 关掉了 NoAction 刹车):

1. `GOAL_IDLE_ROUND_LIMIT`(3):连续 N 轮无实质动作 → `Stop(GoalUnreachable)`。
   条件写得含糊(「优化一下」)或根本达不到时,停下来让用户改条件。
2. D-583 的 `ZeroOutput` 熔断:每轮都调工具但磁盘一个字节没变 → 照常熔断。
   目标 loop **必须先过这道**再谈继续,否则「看起来在干活」的空转无人拦截。
   实现上把它抽成 `note_progress_signature`,常规路径与目标路径共用同一份。

目标是**一次性意图**:达成或判定不可达后后端就地清除,前端同步清空输入框。
不落 localStorage——与「本轮后停」同类(D-111),持久化会让它在下次开应用时
静默复活,驱动一段跟它无关的对话。

### 四、决策点呈现(R-322 / #4)

强度必须在界面上是一个**独立可见控件**,不是藏在 agent 下拉框里的副作用。
用户随时可覆盖模式默认值。

## 技术选型与取舍

| 决策 | 选择 | 取舍 |
| --- | --- | --- |
| 强度载体 | 枚举而非 N 个布尔 | 失去逐项微调,换来「没有两个开关可以互相矛盾」(沿用 phase_pipeline 的精神) |
| 停机权 | 模型声明即终止 | 模型误判「完成」时无人纠正——但用户在场(Paired)本就会纠正,无人值守(Autonomous)有 ZeroOutput/MaxRounds 兜底 |
| 声明通道 | 扩展 `work` 动作 | 不新增工具(D-662),代价是 `work` 的 schema 继续变长 |
| 采集方式 | 事件流 | 与 D-654 一致;不用消息切片(压缩会错位) |
| 调度改造 | 冲突前驱层级调度 | 在「保住冲突对顺序」的约束下已是最优;放弃 first-fit 装箱那种更高并行度,因为它靠重排冲突操作换来,是换语义不是提速 |

## 实施边界与调用方

- `crates/kanzei-harness/src/auto_run.rs`:强度枚举、策略表、`ModelDeclaredDone`、
  判定顺序。**纯逻辑,无 IO**,单测是权威。
- `crates/kanzei-tools/src/work/tool.rs`:`handoff` 动作。
- `crates/kanzei-core/src/runner/tool_exec.rs`:`build_tool_execution_waves_with` 装箱。
- `crates/kanzei-app/src/run/events/mod.rs`:`MetricsSink` 采集 handoff 事件。
- `crates/kanzei-app/src/run/coordinator.rs`:把强度与声明送进 `AutoRunCtx`。
- `crates/kanzei-app/ui/`:强度控件与回显。

**不动**:权限 Ruleset、托管围栏、事件存储 schema、记忆控制平面。

## 变更记录

- 2026-08-21:建档。依据 2026-08-21 外部七点评估与用户逐点定调。
  登记 R-322/R-323/D-661/D-662,先行方案对照见
  `.kanzei/research/r322-prior-art/prior-art.md` 与 `r323-prior-art/prior-art.md`。
- 2026-08-21:R-323 B1 落地。先审计工具并发契约而不是先造模型声明通道——
  `Exclusive` 默认是信息缺失的占位,不是安全断言;4 类工具提升为可并行,
  `websearch` 按入参分流。并记录「声明通道必要性待重估」的理由。
- 2026-08-21:B3 落地。结伴档 loop 的停止规则改为**用户给定的目标条件**
  (参照 Claude Code `/goal`,用户 2026-08-21 定调)。新增 `AutoRunCtx::goal_active`、
  `AutoRunAction::GoalPending`、`AutoStopReason::{GoalMet, GoalUnreachable}`、
  `GOAL_IDLE_ROUND_LIMIT`;`AutoRunController::goal` 持有条件,达成/不可达后自动清除。
  零产出熔断抽成 `note_progress_signature`,目标路径与常规路径共用。
- 2026-08-21:B2 落地。`auto_allowed` 判据由 agent 放宽到 profile,轻档 loop 可达;
  `backlog_stops_loop` 进策略表(结伴档不拿队列状态当停机判据);撤回 R-224 的
  强制切档并反转其冒烟断言。
- 2026-08-21:B1 落地(commit `4f0f46a0`)。实现期更正两处方案文字——
  ①D-661 的解法由「first-fit 装箱」改为「冲突前驱层级调度」,原方案会重排冲突
  操作;②评估者的原始见证用例不成立,已换成 `[A,B,C,D]`+`A↔B`/`C↔D`。
  同时登记 B1 的已知缺口:轻档 loop 在生产路径上还不可达(见 §TODO)。

## 验证证据

B1(commit `4f0f46a0`,workspace 15 个测试二进制全绿,`clippy -D warnings` 干净):

| 验收点 | 锚点 |
| --- | --- |
| 模型声明完成后引擎不再 Nudge(两档一致) | `auto_run.rs::模型声明完成_引擎不再推进_两档一致` |
| 反证:未声明时自主档仍走原刹车 | `auto_run.rs::未声明完成时_自主档仍走原有推进刹车` |
| 结伴档不发 Nudge | `auto_run.rs::结伴档_无动作直接停_不追加推进指令` |
| 结伴档资源类兜底一条不少 | `auto_run.rs::结伴档_资源类兜底一条不少` |
| ZeroOutput 熔断不随强度关闭 | `auto_run.rs::结伴档_零产出熔断仍然生效` |
| 核查轮仅自主档插入、计数两档都累加 | `auto_run.rs::验收核查轮_仅自主档插入_计数两档都累加` |
| 用户意图压过模型声明 | `auto_run.rs::用户意图仍然压过模型声明` |
| 结伴档冗余提醒完全静默 | `redundancy.rs::结伴档_冗余提醒完全静默` |
| handoff 成功才置位、失败不算声明 | `run/events/mod.rs::handoff成功才置位_失败不算声明` |
| work 其它动作不误判为 handoff | `run/events/mod.rs::work其它动作不置位handoff` |
| D-661 不相交冲突链两波 | `tool_exec.rs::切波_不相交冲突对不再各自占一波` |
| D-661 冲突对顺序不颠倒 | `tool_exec.rs::切波_冲突对的先后顺序绝不颠倒` |

B2(workspace 15 个测试二进制全绿,UI 冒烟通过,`clippy -D warnings` 干净):

| 验收点 | 锚点 |
| --- | --- |
| backlog 空只停自主档,不停结伴档 | `auto_run.rs::backlog空_只停自主档_不停结伴档` |
| 轻 loop 形状:有动作续跑、无动作即停 | `auto_run.rs::结伴档轻loop_有动作续跑_无动作即停` |
| 结伴勾鞭挞不再改档位,落轻控制 notice | `scripts/ui-runtime-smoke.mjs` R-322 B2 块 |
| research 勾鞭挞仍拒绝复位 | 同上 |

B3(workspace 15 个测试二进制全绿,UI 冒烟通过,`clippy -D warnings` 干净):

| 验收点 | 锚点 |
| --- | --- |
| 目标挂着时无动作不停,复述条件再推一轮 | `auto_run.rs::目标挂着_无动作不停而是复述目标再推一轮` |
| 达成由模型声明,原因区分于普通交还 | `auto_run.rs::目标达成由模型声明_停止原因区分于普通交还` |
| 连续推不动达上限即停并带轮数 | `auto_run.rs::目标连续推不动_达上限即停并带轮数` |
| 有动作的轮次清零空转计数 | `auto_run.rs::目标空转计数被有动作的轮次清零` |
| 目标压过 backlog 判据 | `auto_run.rs::目标挂着时_backlog不再是停机判据` |
| 零产出熔断仍是兜底 | `auto_run.rs::目标挂着_零产出熔断仍是兜底` |
| 用户意图与资源兜底压过目标 | `auto_run.rs::目标不能压过用户意图与资源兜底` |
| 目标文本归一(空串撤销/超长按字符截断) | `kanzei-app/src/auto_run.rs::目标文本归一_空串撤销_超长截断` |

R-323 B1(工具并发契约审计):

| 验收点 | 锚点 |
| --- | --- |
| 纯检索可并行 | `websearch.rs::concurrency_audit_tests::纯检索可并行` |
| 带 prior-art 主题的检索互斥,且不拖住代码树写入 | `websearch.rs::concurrency_audit_tests::带先行方案主题的检索互斥` |
| 空白主题回落纯读 | `websearch.rs::concurrency_audit_tests::空白主题回落纯读` |

未覆盖:真机端到端验收(留 B4);剩余 9 个 `Exclusive` 工具的逐个审计。

## TODO 与后续风险

- **#6 Memory 复杂度(挂起)**:评估指出记忆控制平面可能让可调试性下降——
  「为什么今天它突然做了这个错误决定」的答案可能是「召回了一条六天前的错误 memory」,
  于是确定性 harness 之上重新引入了大的随机成分。用户定调「暂时无法确定」。
  **观察口径**:R-166 的 F(m) 聚合已在库内(`memory_eval_agg`),
  但离线回放成本高、样本稀疏。在攒够样本前不扩张记忆机制,也不删减。
  重议触发条件:F(m) 样本数达到可判定量,或出现一次可归因到错误召回的现场。
- **#5 D-662 的解法未定**:本文只登记缺陷,不预设方案。可能方向包括合并
  tracker 四件套为单一 `tracker(kind, action)`、或把托管写通道收敛为
  `doc(path, op)` 由引擎按路径分派。改动面大且触及托管围栏的白名单
  (`managed_fence.rs` 的 `MANAGED_WRITERS`),须单独设计。
- **强度与 phase_pipeline 的关系**:`phase_pipeline_enabled` 是现存的第二个开关。
  本轮不合并(避免一次动太多),但 R-322 落地后应评估把它并入强度维度,
  否则「不设第二个开关」的原则名存实亡。
