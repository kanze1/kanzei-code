# kanzei APP 图标设计规范（R-061）

日期：2026-08-10
状态：已落地并验收

> **文档状态(2026-08-10 更新):资产规范与当前正式图标一致。** 对应 R-061 已 done；2026-08-10 用户从三版 memory 方向中选定 B「Memory Layers」并授权覆盖正式资产。

## 设计概念

图标使用一个稳定、克制的 **K 形标记**，表达 kanzei 的 memory-first 工作流：

- 左侧竖笔是稳定的项目与执行主干；
- 右上三条平行层代表可编辑外部记忆持续吸收经验，也对应只读勘察的并行输入；
- 三层在中心逐级压缩、汇入同一个决策点，表达非参数持续学习依靠记忆检索与整理，而非修改模型参数；
- 右下唯一粗笔代表记忆最终服务于一次清晰、高效的开发行动，也与“并行查、串行写”的调度原则一致；
- 几何边缘在 16px/32px 下仍能识别，适合任务栏和托盘；
- 深色底与金色强调对应应用当前中性深灰 + 运行态金色的 UI 设计语言。

## 色彩

| 用途 | 颜色 | 说明 |
|---|---|---|
| 主背景 | `#0d110d` ~ `#1b1e13` | 深色、低饱和，适配深色桌面 |
| 主标记 | `#c9962e` | 金色，表达运行、推进和可见反馈 |
| 辅助高光 | `#ead18a` | 仅用于大尺寸/高 DPI 的边缘高光 |
| 禁用/阴影 | `#151a12` | 保持小尺寸轮廓清晰，不使用纯黑 |

颜色比例约为：背景 70%、主标记 25%、高光 5%。禁止在小尺寸版本加入细文字、渐变噪点或复杂阴影。

## 形状与安全区

- 画布为正方形，主标记占画布 64%～72%；
- 四边安全区至少 14%；背景全幅铺满，不增加边框或第二层圆角矩形；
- 16px/32px：只保留 K 形主体和单层背景；
- 128px 以上：允许保留母版现有的轻微金色层次，但不得增加阴影、发光或纹理；
- 透明度：当前桌面图标使用不透明背景，避免 Windows 任务栏出现浅色棋盘格。

## 资产清单与验收

当前仓库已包含同一设计导出的多平台资产：

- 源图：`crates/kanzei-app/app-icon.png`（1024×1024）
- Tauri 主图：`crates/kanzei-app/icons/icon.png`（512×512）
- Windows：`icon.ico` 及 `Square*Logo.png`
- macOS：`icon.icns`
- Android：`icons/android/mipmap-*`
- iOS：`icons/ios/AppIcon-*`

验收标准：

1. 源图和 `icons/icon.png` 保持同一视觉稿，不能出现背景色/主标记偏移；
2. Windows、macOS、Android、iOS 资产均存在且尺寸由平台命名约束覆盖；
3. 32px/64px 版本仍可辨识 K 形主体；
4. 图标不包含未授权字体、外部品牌标志或用户数据；
5. 设计与 UI 的深灰/金色运行态一致。

## 变更记录

- 2026-08-10：用户选定 B「Memory Layers」方形 K 方案；以 1254px 概念稿经 Lanczos 缩放生成 1024px `app-icon.png`，再用 `cargo tauri icon` 统一覆盖 Windows、macOS、iOS、Android 与通用 PNG 资产。已检查 1024/512/128/64/32px 尺寸，32px 仍可辨认 K 主体。
- 2026-08-10：修复 Windows 可执行文件仍沿用旧图标的问题。`build.rs` 现在显式监听 `icons/icon.ico` 与 `tauri.conf.json`，图标资产变化会强制重新生成并嵌入 PE 资源。

## 后续约束

若重新绘制，只替换源图并重新生成平台资产，不在不同平台手工改色。R-030/R-050 的多进程/线程状态不改变图标主体；运行态可在窗口内部通过状态栏表达，避免同一图标承载过多状态。
