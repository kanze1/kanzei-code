# 记忆收益与任务信息呈现

- 状态：设计基线，第一批代码完成定向验证，整体实施中
- 日期：2026-09-06
- 关联需求：R-361
- 关联缺陷：D-743、D-744
- 关联决策：无新增独立决策

用户先要求从 Agent 视角检查 harness、memory 和前端信息呈现，再确认「同意，开工吧」。本文件记录这一轮方案与实施证据。

## 交付顺序

1. 统一召回明细与运行归属，记录真实正文读取，收紧恢复证据、晋升和效果口径。
2. 改进重复失败重查和决策点召回，使用固定真实任务比较重复探索、重试、用户纠正与成功率。
3. 复用 Work Unit 的有界上下文，整合当前任务、实质进展、记忆来源和验证结果。

第一批代码及定向回归完成。第二、三批和真实 provider 收益对照尚未完成；不能用代码测试证明用户收益。

## 事实契约

- `recall_events` 是召回明细真源。旧 `index.db.memory_recalls` 仅保留历史，不再驱动当前页。
- 检索、注入、正文读取分别记录。读取按 `run_id` 关联到同一运行内最近一次包含该记忆的注入；跨线、历史和手动 UI 搜索不会被追认为读取。读取仅说明 read 工具成功返回该文件的文本，可以是部分内容，不证明理解、采用或阅读完整篇幅。
- 缺少读取观测能力的历史事件显示未知。注入率与读取率均不能作为行为采用或结果改善的替代。
- 排序先按相关性；停止用注入次数充当采纳率加权。仅未读取不足以自动降级记忆。移除基于该代理指标的一键降级 IPC 和按钮，保留逐条查看、修订与状态管理。
- 在线未复发与运行结束按原始含义保留，不再写为 action_changed/outcome_improved。历史 v1 代理记录不作为效果证明。
- 指纹经验晋升须关联对应 episode 中的失败后成功。恢复匹配相同工具与完整目标；读取文件、另一条命令成功或无关轮次不能验证先前修复。

## SQLite v23

新增 `recall_events.run_id`、`read_ids`，均可空。新增 `memory_recoveries` 保存 episode、失败指纹、失败和恢复调用 ID、工具及目标。索引支持运行内定位。

已有召回的新增字段保留 NULL，不推测身份或补造读取。已有文件记忆、事件、评估和来源记录保留；旧在线评估通过查询版本排除。迁移使用 SessionStore 现有事务及升级前备份。回滚旧程序前停用新进程并恢复升级前整库备份；不要仅修改版本号。测试用临时 v22 库验证迁移与幂等，本地用户数据库不主动升级。

## 验收

- 后端：新表有事件、旧表为空时明细仍完整；两条并行运行读取同一记忆不串号；历史 unknown 与实测 false 不混淆；失败后仅读取/不同命令成功不晋升；中断或完成均不能单独证明收益。
- 前端：打开记忆页、默认查看最近一次使用，展开其他召回明细，看到触发原因、注入与读取状态；无命中记录保留；加载失败可重试；切换项目不显示迟到响应。
- 验证：core/memory/app 定向 Rust 回归及迁移测试，UI runtime/i18n/lint/a11y 等相关检查。完整工作区验证、原生桌面验收与实际收益对照分别报告。

## 背景、选择与取舍

原路径中，当前事件写入 state.db，页面读旧 index.db；注入计数又被命名为 fetched 并用于排序及降级。这让数据增加而收益不可判定。修复选择继续使用现有 SQLite、Markdown 和生命周期门禁：新事件记录运行身份与读取事实，历史保留未知。没有采用按时间窗口猜测旧身份或补算历史读取的方案，以免并行运行串线。

恢复判定使用同一 episode 内相同工具与完整目标的失败后成功；相同文件的 read 成功不修复 edit 失败，另一条 bash 命令成功也不修复先前命令。后续同目标再次失败撤销该轮恢复。这个判据保守地验证操作恢复，不能证明记忆内容对恢复有因果贡献；跨工具修复与业务正确性留给独立评估。

## 实施边界与调用方

- core：SQLite v23、观测查询、run_id 关联、读取事实和恢复来源；在线旧代理记录从收益证据中排除。
- memory：运行提示逐轮组装，停止跨运行 30 分钟去重；停止注入率加权；统一工具统计和人工复查口径；promote 校验对应恢复。
- CLI/桌面：工具上下文、召回与 episode 使用相同 run_id，episode 写入后保存该轮工具恢复。
- UI：当前表明细含 miss，区分注入、实测读取与历史未知，默认展示最近使用并折叠其他检索，项目切换与加载重试有回归。

## 验证证据与复测用例

所有以下测试使用临时库/测试运行或模拟前端数据；未安装新桌面程序。

| 验证入口 | 测试与预期 | 结果 |
| --- | --- | --- |
| `cargo test -p kanzei-core --lib --offline -- --test-threads=1` | v22 缺列迁移、幂等、run 隔离、历史 NULL、miss、手动搜索、恢复配对、在线旧代理不算收益 | 284 项通过 |
| `cargo test -p kanzei-memory --lib --offline -- --test-threads=1` | 晋升先拒无恢复再接受匹配来源；排序不随注入计数变化；每轮提示；读取记录 | 168 项通过 |
| `cargo test -p kanzei-tools --lib read::tests --offline -- --test-threads=1` | 真 ReadTool 成功读取回填同运行，失败读取不回填，旧表不追认 | 10 项通过 |
| `cargo test -p kanzei-app --bin kzapp memory::tests --offline -- --test-threads=1` | 新表独立提供 IPC 数据、历史未知/实测 false 分开、损坏观测报错、来源缺口 | 2 项通过 |
| `cargo check -p kanzei-app -p kanzei --tests --offline` | 前后端 IPC 注册及 CLI/桌面调用链编译 | 通过 |
| 前端六项检查 | ui-runtime、ui-lint、parallel-lines-regression、ui-a11y、ui-i18n、ui-markdown；覆盖新字段、未知、miss、失败重试及迟到响应 | 全部通过 |
| `cargo test -p kanzei --test integration memory_hints_not_persisted --offline` | 提示只存在于当前运行上下文、不会累积回灌历史 | 2 项通过 |
| 其他前端检查 | IPC emit/listen 求差、工作空间浏览器回归、窄窗口布局 | 通过 |

前端操作验收：进入记忆页，最新检索默认展开，其余逐项展开；旧观测显示读取未知，未命中仍可查看检索词；发生加载错误时提示失败且能重试；快速切换项目时旧请求不得覆盖当前项目。

## TODO 与验证边界

1. 发布准备同步修复两个既有 CI 测试告警：`tracker/fields.rs` 的生产函数移至测试模块前，`tracker.rs` 移除多余可变引用；不改变业务行为。`cargo clippy --workspace --all-targets -- -D warnings` 已通过；发布前继续执行完整验证。
2. 用户于 2026-09-06 明确授权「直接发版就行」。完整 `scripts/verify.ps1 -Full`、打包、发布和安装校验进入发布流程；实际结果以对应提交的 `dist/verification.json`、发布日志及 GitHub Release 为准。原生桌面交互与实际收益单独验收。
3. 第二批：重复失败时真正改变重查策略，围绕决策点供给记忆；固定真实任务做开启/关闭记忆对照，测重试、重复探索、用户纠正、成功率与耗时。
4. 第三批：结伴/自主职责分离，Work Unit 有界上下文与实质进展判定，前端整合当前任务、依据和验证结果。
5. 本轮查询旧 CLI 的 `kz test --help` 意外落入通用运行入口，已中止；命令解析将未知子命令作为提示的行为应另行评估，未在本批修改。

## 变更记录

- 2026-09-06：按用户确认建立三批实施顺序；首批接通真实观测、收紧恢复证据、修正页面与统计语义，补迁移和前后端回归。分支 `kanzei/memory-feedback`；用户授权首批发版，合并与发布结果由发布记录留证。
