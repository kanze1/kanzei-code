# OC 音色方向

更新：2026-09-24。

## 用户确认

- 冷脸萌，偏中性，带冷淡假小子的感觉。
- 加入木下秀吉的声音作为参考。
- 当前样片声音与角色不匹配，重新选音色素材。
- 2026-09-24 在音色项目试听后选定 C「低声利落」，以该版作为当前接入方向。选择记录为 `C:/Users/kanzei/Documents/ChatGPT/kanzei音色/deliverables/oc-cn-20260924/selection.json`。

## 参考与取舍

下面保留初始音色参考。三组已经完成对照，用户最终选择两仪式方向的 C；木下秀吉与奇诺保留为对照素材。

| 参考 | 出处与配音 | 建议取用 |
| --- | --- | --- |
| 木下秀吉 | 《笨蛋，测验，召唤兽》，加藤英美里 | 清亮、轻柔的中性少年感 |
| 奇诺（2017） | 《奇诺之旅》，悠木碧 | 平静、短句、自然的中性表达，作为说话方式的优先比较对象 |
| 两仪式 | 《空之境界》，坂本真绫 | 更低、更利落、略有锋芒的语气 |

当前建议的方向：中等偏低的女性／中性少年声区，声音清楚，气息适度，起伏小，句尾收住。日常回答简短自然；吐槽留半拍，喜欢的事情让声音稍微放松。

先按角色各收集 30–60 秒干净、正常音量的单人日常对白，分开保存，用于音色小样比较。这是试听材料量，正式训练的数据量待选定模型与方案后确定。先比较单一主音色，再决定如何体现其他参考的表演特征。

## 当前接入（2026-09-24）

交付目录：`C:/Users/kanzei/Documents/ChatGPT/kanzei音色/deliverables/oc-cn-20260924`。当前入口为 `selected/`，`recommended/` 是旧 B 版留档。

当前 C 使用 Qwen3-TTS Base 与 28.43 秒两仪式日语参考，`x_vector_only_mode=true`；没有新增微调权重，也没有将三个角色混合训练。运行服务返回的参考 SHA256 与用户选定 C 一致：`d514364ac6d714134b9d26b6428784b7905e78ec7f3337e2c4853287b3ec1cc4`。

本次复核了清单中的 411 个文件，大小与哈希全部一致。C 的原始八条验收音频、预热音频、参考及连听文件均为 24 kHz 单声道 PCM16，无削波。另经实际 `/speak` 生成两段新中文，预热后的可播放缓冲就绪时间为 0.56–0.65 秒；主软件原有 PCM 解码器正确处理了实际 HTTP 分块中的奇数字节边界。

主软件用户配置 `C:/Users/kanzei/.kanzei/voice.json` 已切到端口 7389，语言保留 `auto`。切换后使用该配置再次实际生成语音，服务身份与参考校验一致，PCM 解码通过；这次缓冲就绪约 0.69 秒。复核记录在 `output/voice/oc-c-review-20260924/`。尚未完成 Tauri 麦克风到扬声器的整机测试。

服务启动：在交付目录运行 `python runtime/manage.py start`。恢复旧设置：运行交付目录 `Use-In-Kanzei.ps1 -Restore`。服务及模型仍由音色项目的独立运行环境提供。

机器转写在新短句中没有识别开头的“嗯”，将术语 `Tauri` 写为 `Tori`；其余正文、数字和 `Rust` 对应正确。上述两处需要听感复核，机器转写不能单独判定发音错误。音色自然度沿用用户对 C 的试听选择，本次仅验证文件、接口、信号与识别结果。

OC 预览及 `output/oc-film/kanzei-character-v4-30s.mp4` 使用 C 音色。30 秒音轨为 `assets/oc/demo-speech-c-v3.wav`，包含倾听、解释、吐槽和完成四段台词；解释台词由当前服务新生成，其余三段取自用户选定 C 的验收材料。各段只做固定增益调整与时间排布，来源、时间和哈希记录在 [voice-v3-manifest.json](oc-references/voice-v3-manifest.json)。实时预览依实际播放音频的幅度驱动嘴型，视频导出依同一 WAV 的 PCM 幅度逐帧渲染。

## 固定试听台词

试听台词：

1. “嗯，我在。你继续说。”
2. “问题在这里。先改这一处。”
3. “流程挺完整。连多做一遍都安排好了。”
4. “刚做的一小段。……你听听。”
5. “你听出来了？……嗯，我在这里花了挺久。”

官方出演信息：[木下秀吉](https://kadokawa-animation.jp/lineup/bakatest)、[奇诺](https://www.kinonotabi-anime.com/character/)、[两仪式](https://www.aniplex.co.jp/lineup/karanokyoukai/)。
