# 前端三期:能力差距全面分析(2026-08-06)

背景:后端能力(多轮调度、子代理、持久化、双模式、权限体系)已明显跑在前端前面。
本文盘点"后端有、前端没有/没跟上"的全部差距,按用户的好用定义排序:
①memory/上下文精准透明 ②方便多 agent 协作 ③快和流畅 ④少打断 ⑤信息清晰。

## 一、后端能力 vs 前端呈现矩阵

| 后端能力 | 前端现状 | 差距 |
|---|---|---|
| steer/queue 双投递 + 队列取消 | 有 delivery 下拉 + 停止取消 | **队列不可见**:排了几条、内容是什么、不能单条撤销 |
| task 并行子代理 + 实时进度 | 块内进度行 + 后台面板 | **轨迹不可回看**:只见结果摘要,子代理内部做了什么(哪些 grep/read)无法展开;后台面板条目 6s 即逝 |
| SessionStore 事件溯源/会话恢复 | 历史对话列表(R-013 进行中) | 回放/继续的交互还在建设 |
| 自动压缩 + 手动总结 | 一条 notice | **纪要不可见**:压缩后模型看到什么无处查看,违背"上下文透明" |
| 权限规则持久化(always-allow) | 无 | R-025:看不到记住了什么,只能手改 toml |
| research 模式(sources/findings 强制引用) | **完全没有 UI** | 侧边栏只有 dev 模式文档;research 的来源/发现列表、引用关系无呈现 |
| 多模态协议(image/document) | 无上传入口 | R-014/R-024 |
| git 集成 | 状态栏一行 | 无提交列表;agent 现在会自动提交,"它提交了什么"要去终端看 |
| 上下文占用计量 | 状态栏百分比 | 无成分拆解(system/工具/历史各占多少) |
| 多进程并行(R-030 设计完) | 单运行位 | 见 r030-process-decoupling.md |

## 二、纯前端易用性债务

1. **强制滚底**:每个事件都 scrollBottom,翻历史时被拽走——需要"在底部才跟随"的智能滚动 + 「回到最新」按钮(④少打断,最高频痛点)
2. 消息不可一键复制;对话内无搜索
3. 权限弹窗一次只显示一个,多 ask 排队时无队列感
4. 连跑控制粗糙:上限 10 写死、无暂停、无"跑完这轮就停"
5. 输入框:无提示词历史、无草稿保存(R-024)
6. diff:无语法高亮、无并排、多文件改动无汇总视图
7. 无键盘快捷键体系(切进程/停止/新对话)
8. 错误一律全屏红条,不区分可重试/致命

## 三、需求整理(已入库)

- **R-030** 进程与项目解耦:多进程并行、每进程模型+子代理开关(P1 后端→P2 页签→P3 持久化)——大手术,Claude 主刀
- **R-031** 子代理轨迹透视:task 块可展开子代理完整工具轨迹;后台面板历史可回看
  - 实时阶段已完成:TaskProgress 携带子工具 start/end 结构化事件,task 条目可展开查看子工具状态/预览/display,活动面板跨轮保留。
  - 历史回放已完成:运行轨迹写入 run.trace 事件,历史会话加载时按对话分段恢复右侧 task/子工具轨迹。
- **R-032** 队列可视化:排队输入列表(内容/交付方式)+ 单条撤销
- **R-033** 阅读体验:智能滚动跟随 + 回到最新、消息复制、对话内搜索
- **R-034** research 模式前端:来源/发现侧边栏、引用跳转、报告入口
- **R-035** diff 查看器升级:语法高亮、并排视图、多文件改动汇总
- 既有关联:R-013(会话回放,agent 进行中)、R-018(完成提示音)、R-024(输入体验)、R-025(权限规则管理)、R-016(自更新)

## 四、需求整理补充(2026-08-07,配色收尾时登记)

§二易用性债务中此前未入库的 4 项,已登记为:

- **R-039** 权限弹窗队列化 + 连跑控制增强(暂停/跑完本轮停/上限可配)——§二.3/§二.4
  - 已完成:权限弹窗显示当前/总数与待处理预览;连跑支持暂停/恢复、本轮后停止、1-100 上限持久化;停止会取消旧续跑 timer。
- **R-040** 键盘快捷键体系(停止/新对话/切进程/聚焦输入框)——§二.7
- **R-041** 错误分级展示:可重试与致命分离——§二.8
- **R-042** 上下文成分拆解 + 压缩纪要可查看——§一(上下文透明)

至此 §二 8 项债务全部有归属:1/2→R-033(done)、5→R-024、6→R-035、3/4/7/8→R-039~R-041。

## 五、R-043 项目管理落地(2026-08-07)

R-043 第一阶段已落地:项目列表保留旧版路径数组兼容,新增名称映射;支持初始化不存在的项目目录、持久化项目显示名与重命名;移除项目需要确认且不删除磁盘文件。新增/初始化/切换/移除当前项目后,前端统一刷新会话、文档、模型、Git、排队输入和右侧面板上下文。

后续 TODO:初始化向导的基础配置表单、项目级配置显式编辑，以及更完整的跨项目运行隔离回归。

## 六、后续用户体验与并行协作需求(2026-08-07)

| ID | 需求 | 优先级 | 复杂度 | 备注 |
|---|---|---:|---:|---|
| R-045 | 全局工作区管理:项目/对话统一上层目录与运行状态 | P1 | 中 | 对话/设置/项目统一入口 |
| R-046 | 统一连续推进与过夜模式:可配置 turn 上限 | P1 | 中 | 连跑与自主推进合并 |
| R-047 | 对话内复制上下文与小总结收纳 | P2 | 小 | 顶栏复制入口下移,总结归右侧活动 |
| R-048 | 对话尾部运行指示与计时器 | P1 | 小 | 固定状态区,不随事件闪烁 |
| R-049 | 缺陷梳理:静态 harness 分析报告 | P1 | 中 | 只读报告,动态分析后续再做 |
| R-050 | 并行对话线程与分支工作树:隔离运行、冲突检测与合并 | P1 | 大 | 先设计/只读 POC,再 worktree |
| R-051 | 需求复杂度分级:小/中/大 | P2 | 小 | 与现有 P0-P3 优先级并列 |

### R-050 复杂度评估与实施边界

R-050 是架构级改动，不能用“多开两个前端 Tab”替代。线程至少要隔离:

- 对话历史与会话恢复
- runner/停止句柄与运行状态
- 权限询问队列及回答路由
- 输入队列、活动轨迹和 token 状态
- 文件写入、git 提交和项目级锁

建议分三阶段:①同一项目双线程只读 POC,验证状态/权限/取消隔离;②每线程独立 git worktree/分支,写入只落在自己的工作树;③提供 diff 审查、冲突检测、选择性合并、失败保留与恢复入口。锁顺序和崩溃恢复必须在实现前写成设计约束,否则容易出现死锁、串权限或文件互相覆盖。


## 七、建议实施顺序

1. R-030(解耦是其他一切的地基;页签机制与 R-013 回放共用 pane)
2. R-033(最高频的日常摩擦,小工作量大收益)
3. R-031 + R-032(多 agent 协作的可观测性)
4. R-034(research 模式启用的前提)
5. R-035 / R-024 / R-025 / R-018(按需)

## 八、R-050 只读 POC 前置设计（2026-08-09）

R-050 在 R-030 的进程/session 隔离契约落地前，只推进设计和只读验证，不创建 worktree、不执行并行写入或自动合并。

### 线程—项目—session—worktree 关系

- `project` 是共享文档、规范和权限规则的资源边界。
- `process` 由 R-030 提供，是绑定项目的独立运行容器；同一项目可拥有多个 process。
- `thread` 是用户可见的并行对话线程，必须绑定一个 process，并拥有独立消息投影、运行句柄、停止边界、权限队列、输入队列和活动轨迹。
- `session_id` 是事件、队列和历史恢复的唯一归属键；线程不得只用 `project_dir` 路由事件。
- `worktree` 仅在只读 POC 之后启用：主线程可使用默认工作树，写线程必须绑定独立 worktree/分支；合并前只能生成 diff，不得自动覆盖主工作树。

### 线程状态机与边界

```text
created -> idle -> running -> stopping -> idle
                         └-> failed -> idle
created/idle -> closed
```

- `stop(thread_id)` 只取消该线程的 runner、pending ask、steer 和 queue，不得影响同项目其他线程。
- `closed` 线程拒绝新输入；运行中的线程必须先进入 `stopping`，等待句柄收尾后再释放资源。
- 崩溃恢复按 `session_id` 重放最后一致事件：未完成运行恢复为 `failed/recoverable`，不得伪造为成功；待处理权限询问默认失效并要求重新发起。

### 锁顺序与并发规则

锁顺序固定为：`thread lifecycle -> session admission -> project write lock -> git/worktree lock`；禁止反向获取。

- 只读线程不得持有项目写锁，允许并行读取。
- 需求/缺陷/目标整文件重写和 git 操作必须经过项目写锁；检测到版本/哈希变化时拒绝静默覆盖并返回冲突。
- worktree 创建、合并和清理必须在 git/worktree 锁内完成，失败时保留双方分支和恢复入口。
- POC 阶段只验证消息、权限、队列、活动事件和停止隔离，不写项目文件、不提交 git。

### 双线程只读 POC 验收矩阵

| 场景 | 线程 A | 线程 B | 通过条件 |
|---|---|---|---|
| 消息隔离 | 发送 prompt A | 发送 prompt B | 两侧只出现自己的 user/assistant 消息，历史恢复不串线 |
| 运行隔离 | running + stop A | 持续运行 B | stop A 后 B 仍运行，事件只路由到对应线程 |
| 权限隔离 | 产生 ask A | 产生 ask B | 回答 A 不改变 B 的 ask；关闭 A 只清理 A |
| 队列隔离 | admission/steer A | admission/queue B | FIFO、取消和 drain 只作用于对应 session_id |
| 活动隔离 | task/工具轨迹 A | task/工具轨迹 B | 面板可按线程过滤，轨迹和失败状态不互相覆盖 |
| 崩溃恢复 | 中断 A | B 正常结束 | A 恢复为 failed/recoverable，B 保持成功，均可继续操作 |
| 只读门禁 | 请求写文件/git | 正常读取 | POC 拒绝写入并保持主工作树与项目文档无变化 |

### 后续入口

待 R-030 确定 `process_id/session_id` 命令与事件契约后，先实现上述 POC 的内存态双线程测试，再接入 worktree、diff 审查和冲突合并；在此之前不得实现 R-050 的并行写入流程。
